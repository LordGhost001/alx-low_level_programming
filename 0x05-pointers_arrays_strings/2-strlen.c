#include "main.h"
#include <string.h>

/**
* _strlen - gets the length of a string
* @s: arg to calc length of its string
* Return: length of the string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
