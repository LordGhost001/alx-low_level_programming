#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: the number to be checked
* Return: Always 0 (Always)
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
